{
  "name": "rikeich-q",
  "private": true,
  "scripts": {
    "bump-cache": "node scripts/bump-sw-cache-version.js",
    "deploy": "npm run bump-cache && firebase deploy",
    "check-paths": "node scripts/check-explanation-paths.js"
  }
}
